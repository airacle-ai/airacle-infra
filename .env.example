# Airacle 项目级 .env 模板
# 复制到 projects/<name>/.env，本文件加载在 ~/.airacle/secrets.env 之后
#
# ⚠️ 重要：API keys 全部走中央 secrets 文件 ~/.airacle/secrets.env
#    不要在这里声明 ANTHROPIC_API_KEY / OPENAI_API_KEY / GITHUB_TOKEN 等。
#    docker-compose env_file 后者覆盖前者，即使写 `KEY=`（空值）也会把
#    中央 secrets 里的真值清空 → "Invalid API key" 错误。
#    详见 docs/08-lessons-learned.md 坑 14。
#
# 本文件只放：本项目特定的、非密的参数

# Claude 默认模型（可选，不填使用 Claude Code 自己的默认）
# CLAUDE_MODEL=claude-sonnet-4-5

# 项目可能想覆盖的其它非密设置...
